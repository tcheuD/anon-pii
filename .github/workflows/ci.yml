# CI Workflow for anon-pii
# Issues: #1, #2, #3, #4, #28, #29, #34
name: CI

on:
  push:
    branches: [main]
    paths-ignore:
      - '**.md'
      - 'docs/**'
  pull_request:
    branches: [main]
    paths-ignore:
      - '**.md'
      - 'docs/**'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

# Public feature coverage policy:
# - Required PR gate: default, ner-lite+proxy, xlsx, and pdf tests.
# - Main/manual only: image OCR tests, because they require Tesseract packages.
# - Excluded from required CI: ML ner, because it requires ONNX Runtime and ORT_DYLIB_PATH.
#   These exclusions are not part of the required PR gate.

jobs:
  # ============================================================================
  # Rust Format Check
  # ============================================================================
  rust-fmt:
    name: Rust Format
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt

      - name: Check formatting
        run: cargo fmt --all --check

  # ============================================================================
  # Rust Clippy Lint (lightweight PR gate)
  # ============================================================================
  rust-clippy:
    name: Rust Clippy
    if: github.actor != 'dependabot[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy

      - name: Cache
        uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
        with:
          prefix-key: v1-clippy

      - name: Clippy (default features)
        run: cargo clippy -- -D warnings

      - name: Clippy (all features except ner)
        run: cargo clippy --features ner-lite,proxy -- -D warnings

  # ============================================================================
  # Rust Tests (lightweight PR gate)
  # ============================================================================
  rust-test:
    name: Rust Test (${{ matrix.features || 'default' }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        features: ['', 'ner-lite,proxy']
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache
        uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
        with:
          prefix-key: v1-test
          shared-key: ${{ matrix.features || 'default' }}

      - name: Run tests
        run: |
          if [ -n "${{ matrix.features }}" ]; then
            cargo test --features "${{ matrix.features }}"
          else
            cargo test
          fi

  rust-test-macos:
    name: Rust Test macOS (${{ matrix.features || 'default' }})
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: macos-latest
    strategy:
      fail-fast: false
      matrix:
        features: ['', 'ner-lite,proxy']
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache
        uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
        with:
          prefix-key: v1-test
          shared-key: ${{ matrix.features || 'default' }}

      - name: Run tests
        run: |
          if [ -n "${{ matrix.features }}" ]; then
            cargo test --features "${{ matrix.features }}"
          else
            cargo test
          fi

  # ============================================================================
  # Rust Tests — XLSX
  # ============================================================================
  rust-test-xlsx:
    name: Rust Test (xlsx)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache
        uses: Swatinem/rust-cache@779680da715d629ac1d338a641029a2f4372abb5 # v2.8.2
        with:
          prefix-key: v1-test
          shared-key: xlsx

      - name: Run tests (xlsx)
        run: cargo test --features xlsx

  # ============================================================================
  # Rust Tests — PDF
  # ============================================================================
  rust-test-pdf:
    name: Rust Test (pdf)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache
        uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
        with:
          prefix-key: v1-test
          shared-key: pdf

      - name: Run tests (pdf)
        run: cargo test --features pdf

  # ============================================================================
  # Rust Tests — Image/OCR (main/manual only; requires Tesseract)
  # ============================================================================
  rust-test-image:
    name: Rust Test (image)
    if: github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Install Tesseract
        run: sudo apt-get update && sudo apt-get install -y tesseract-ocr libtesseract-dev libleptonica-dev

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache
        uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
        with:
          prefix-key: v1-test
          shared-key: image

      - name: Run tests (image)
        run: cargo test --features image

      - name: Run ignored tests (image, requires Tesseract)
        run: cargo test --features image -- --ignored

  # ============================================================================
  # Rust Build (ubuntu — always, macOS — main only)
  # ============================================================================
  rust-build:
    name: Rust Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache
        uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
        with:
          prefix-key: v1-build

      - name: Build release
        run: cargo build --release

  rust-build-macos:
    name: Rust Build macOS
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache
        uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
        with:
          prefix-key: v1-build

      - name: Build release
        run: cargo build --release

  # ============================================================================
  # Security: cargo-deny
  # ============================================================================
  security-deny:
    name: Security (cargo-deny)
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Install cargo-deny
        uses: taiki-e/install-action@787505cde8a44ea468a00478fe52baf23b15bccd # v2.75.21
        with:
          tool: cargo-deny

      - name: Check advisories
        run: cargo deny check advisories

      - name: Check licenses
        run: cargo deny check licenses

      - name: Check bans
        run: cargo deny check bans

      - name: Check sources
        run: cargo deny check sources

  # ============================================================================
  # Security: cargo-audit
  # ============================================================================
  security-audit:
    name: Security (cargo-audit)
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Install cargo-audit
        run: |
          curl -sSL https://github.com/rustsec/rustsec/releases/download/cargo-audit%2Fv0.22.1/cargo-audit-x86_64-unknown-linux-gnu-v0.22.1.tgz | tar xz --strip-components=1
          mv cargo-audit /usr/local/bin/

      - name: Run audit
        run: cargo-audit audit

  # ============================================================================
  # Summary Gate
  # ============================================================================
  ci-success:
    name: CI Success
    needs:
      - rust-fmt
      - rust-clippy
      - rust-test
      - rust-test-xlsx
      - rust-test-pdf
      - rust-test-macos
      - rust-test-image
      - rust-build
      - rust-build-macos
      - security-deny
      - security-audit
    runs-on: ubuntu-latest
    if: always()
    steps:
      - name: Check all jobs
        run: |
          # Always required
          for job in rust-fmt rust-test rust-test-xlsx rust-test-pdf rust-build; do
            result="${{ needs.rust-fmt.result }}"
            case "$job" in
              rust-test) result="${{ needs.rust-test.result }}" ;;
              rust-test-xlsx) result="${{ needs.rust-test-xlsx.result }}" ;;
              rust-test-pdf) result="${{ needs.rust-test-pdf.result }}" ;;
              rust-build) result="${{ needs.rust-build.result }}" ;;
            esac
            if [[ "$result" != "success" ]]; then
              echo "Required job $job failed: $result"
              exit 1
            fi
          done
          # May be skipped (main-only or dependabot-skip)
          for result in "${{ needs.rust-clippy.result }}" \
                        "${{ needs.rust-test-image.result }}" \
                        "${{ needs.security-deny.result }}" \
                        "${{ needs.security-audit.result }}" \
                        "${{ needs.rust-test-macos.result }}" \
                        "${{ needs.rust-build-macos.result }}"; do
            if [[ "$result" != "success" && "$result" != "skipped" ]]; then
              echo "Optional job failed: $result"
              exit 1
            fi
          done
          echo "All CI jobs passed!"
